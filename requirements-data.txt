# Dataset-rebuild dependencies -- ONLY needed to regenerate the training data
# from scratch via `python get_files.py && python get_inputs_outputs.py`.
#
# processed_dataset/ is committed to this repo, so normal training and eval
# never import any of these. Skip this file on a GPU pod.
biopython>=1.81      # get_files.py (PDBList), get_inputs_outputs.py (MMCIFParser)
requests>=2.31       # get_files.py -- the RCSB search API call
scipy>=1.10          # get_inputs_outputs.py -- cdist for the CA-CA distance matrix
